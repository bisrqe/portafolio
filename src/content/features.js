// Site sections that can be switched off without deleting their code or content.
// Set a flag back to `true` to show the section again (links, pages and sitemap entries).
export const FEATURES = {
  // Timeless FTS photography sub-site (/timelessfts) and its Instagram link
  photography: false,
}
